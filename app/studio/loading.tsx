import { AppLoadingScreen } from "@/components/ui/AppLoadingScreen";

export default function StudioLoading() {
  return (
    <AppLoadingScreen
      title="Loading studio..."
      description="Preparing the design canvas, template data, and live preview."
    />
  );
}
